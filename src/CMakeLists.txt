file(GLOB MY_HEADERS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.h *.hpp)
file(GLOB MY_SOURCES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.cpp)

rock_library(
  ${PROJECT_NAME}
  SOURCES ${MY_SOURCES}
  HEADERS ${MY_HEADERS}
  DEPS_PKGCONFIG soem base-lib
)
